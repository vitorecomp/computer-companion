#include <scheduler.hpp>

void get_schedules(struct schedules_runs *runs)
{
    // get the schedules
}

int scheduler_get_commands(struct command *commands, struct schedules_runs *runs)
{
    // get the commands
    return 0;
}